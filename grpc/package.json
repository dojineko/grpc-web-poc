{
  "name": "grpc",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "protoc --js_out=import_style=commonjs:js --grpc-web_out=import_style=typescript,mode=grpcwebtext:js --go_out=go --go_opt=paths=source_relative --go-grpc_out=go --go-grpc_opt=paths=source_relative --proto_path=src src/*.proto"
  },
  "devDependencies": {}
}
